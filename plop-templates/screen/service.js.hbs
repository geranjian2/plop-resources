export class Create{{pascalCase name}}Service {}

import { Injectable } from '@nestjs/common';
import { {{pascalCase name}}Entity } from '../entity/{{pascalCase name}}Entity';

@Injectable()
export class {{pascalCase name}}Service {
  constructor(
    @InjectRepository({{pascalCase name}}Entity)
    private readonly documentSupport: Repository<{{pascalCase name}}Entity>,) {}
  async login(user: any) {
    const { id, ...rest } = user;
    const payload = { sub: id };

    return {
      ...rest,
      accessToken: this.jwtService.sign(payload),
    };
  }
}
