

import { Inject, Injectable } from '@nestjs/common'
import { {{pascalCase name}}Model } from 'apps/{{name}}/src/domain/entities'
import { I{{pascalCase name}}Repository } from 'apps/{{name}}/src/domain/repositories'

@Injectable()
export class Create{{pascalCase name}}UseCase {
  constructor(
    @Inject('{{pascalCase name}}Repository') private userRepository: I{{pascalCase name}}Repository,
  ) {}
  async run():Promise<any> {
    
    }
}
